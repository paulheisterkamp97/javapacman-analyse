plugins {
    id "org.sonarqube" version "3.0"
    id "java"
    id 'application'
}
mainClassName = 'Pacman'

sonarqube {
    properties {
        property "sonar.projectKey", "javapacman-analyze"
        property "sonar.organization", "paulheisterkamp97"
        property "sonar.host.url", "https://sonarcloud.io"
    }
}
